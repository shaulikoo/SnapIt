
var exec = require('exec');


module.exports={
    pics : function pics(){
            exec(['./photo.sh'], function(err, out, code) {
            if (err instanceof Error)
                throw err;
            process.stderr.write(err);
            process.stdout.write(out);
            return 1;
            })
        },

    pico : function pico(){
           return 1;
        }
}