var exec = require('exec');
var PythonShell = require('python-shell');

module.exports={
    pics : function pics(){
        var status=1
        PythonShell.run('Picture.py',options ,function (err) {
            if (err)
                status=0
        });
        return status;
    },
    pico : function pico(){
        return 1;
    }
}
