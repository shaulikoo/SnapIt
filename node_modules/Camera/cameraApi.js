var PythonShell = require('python-shell');

var options = {
    mode: 'json',
    pythonOptions: ['-u'],
    scriptPath: '/home/pi/pywork',
    args: []
};

module.exports={
    pics : function pics() {
        var shell = new PythonShell('shooting_test.py', options);
        data = shell.receive(data);
        console.log(data)
    }



    pico : function pico(){
        return 1;
    }
}
